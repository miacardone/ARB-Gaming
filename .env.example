# Active tenant. Selects the brand config in src/brand/brand.config.js.
# Everything tenant-specific — palette, logo, currency, locale, vocabulary,
# entities, queues, reason codes — follows from this one value.
# arb | pch
VITE_TENANT=arb
